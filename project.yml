name: AL-ZKER
options:
  bundleIdPrefix: com.ftomaamotf
targets:
  AL-ZKER:
    type: application
    platform: iOS
    deploymentTarget: "16.0"
    sources:
      - path: .
        includes:
          - "*.swift"
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.ftomaamotf.alzker
        INFOPLIST_KEY_UILaunchScreen_Generation: YES
